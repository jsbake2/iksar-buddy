# Session Log — 2026-06-12

## Scope this session
Dashboard layout polish, fix the in-browser SPICE console (clicks + stuck-Alt),
per-member follow, buff targeting, and get the dialog-accept helpers (revive,
quest) actually working end-to-end. Scrap the mender/armor-repair feature.

## Changed
- `brain/web/static/index.html` — panel `order:` reflow: Live view top-left,
  Manual control top-right (tall), **State & run pulled into its own panel** under
  the console, Group/Self/Sensors/Status stacked left, Group HP graph moved to the
  bottom. Removed `follow` from Movement and the `repair armor` accept button.
  Collapsed buff1/buff2 into a single `buff` button.
- `brain/web/static/themes.css` — `.grid` got `grid-auto-flow:row dense; align-items:start`
  and `.control-panel{grid-row:span 4}` so short left panels stack under the console
  instead of leaving dead space. Added `.mfollow` button + `.member .id` flex column.
  Renamed `live-rdp-hint` → `live-console-hint` (it was never RDP).
- `brain/web/static/spice/src/spicemsg.js` — **click-coordinate fix**: `SpiceMsgcMousePosition`
  / `MouseMotion` now scale `offsetX/Y` by the canvas buffer/display ratio. The canvas
  is CSS-stretched to the window but its buffer is guest-res, so clicks were sent at the
  wrong scaled coords (landing on the chat bar → "opens chat as if I pressed Enter").
- `brain/web/static/spice/src/inputs.js` + `display.js` — **stuck-Alt fix**: new
  `handle_blur` releases all modifiers (Alt/Ctrl/Shift/Meta) when the canvas loses
  focus, wired on the canvas `blur` event. Alt+Tab / clicking away was stranding the
  Alt-up so the guest held Alt forever.
- `brain/web/static/app.js` — per-member `follow` button wired to `/api/act/follow/<slot>`,
  hidden on the healer slot.
- `brain/web/app.py` — `follow` added to `_MEMBER_ACTIONS` (targets F# then autofollow).
  buff_tank/dps/self now resolve the slot from `slot_roles` and pass `target_slot` so
  the agent targets that member before casting. New `buff` group action sequences
  buff1+buff2 (`,pause_1.5,`). `_ACCEPT` entries are now argv lists; **quest passes
  `--accept`**. Dropped the dead `repair` branch.
- `infra/vm/ahk/key_ev.ahk` — **guest-side stuck-modifier hardening**: release
  `{Alt up}{Ctrl up}{Shift up}{LWin up}` at start of every run (clears anything a
  `#SingleInstance Force` kill stranded mid-combo) + `OnExit(ReleaseMods)`.
- `infra/vm/host-helpers/revive_accept.py` — gate rewritten for the REAL prompt
  ("<caster> would like to cast '<spell>' on you. Do you accept?" — note: contains no
  "revive"/"resurrect"). Clicks **Yes**, never the death-window **Revive** (which would
  forfeit the rez). Added internal OCR retry loop (Yes only renders on some frames).
- `infra/vm/host-helpers/quest_accept.py` — `--accept` manual mode bypasses the
  allow/deny policy (the policy is for autonomous use only).
- `infra/vm/host-helpers/burst_capture.sh` — NEW. Full-res `virsh screenshot` burst to
  `~/ib-data/captures/<label>/` for capturing one-shot/short-lived UIs.
- `infra/vm/host-helpers/push_guest_file.sh` — NEW. base64 → `WriteAllBytes` push of a
  local file into the guest via the qemu agent (how key_ev.ahk now gets deployed).

## Decisions made
- SPICE console is correct transport (passive, doesn't detach console). Both its bugs
  were client-side (coord scaling, modifier-on-blur), fixed in the vendored client.
- Per-member follow stacked UNDER the role dropdown (not inline) — the id column is 130px.
- Mender/armor-repair feature SCRAPPED at owner's call — no clean keyboard way to open
  the vendor window without the mouse; he'll click repair manually.
- Hail (`Ctrl+5`) "scrolls the hotbar" is NOT a bot bug: the injector sends a clean
  Ctrl+5 (log-confirmed) but that collides with EQ2's hotbar page-change binding on his
  stacked bars. Owner's in-game config to resolve.

## State now
- Working: revive-accept (validated live — Spirit Guide rez → clicked Yes → "regain
  consciousness"). quest-accept (validated live — "Reporting for Duty" accepted via the
  dashboard `--accept` path). SPICE coord + blur fixes deployed. AHK + SPICE modifier
  hardening deployed (AHK pushed into guest, 2812 bytes, verified). 27 tests pass.
- Broken / known issues: Hail/sow/item_use on Ctrl+# collide with his hotbar paging
  (owner fix). SPICE view may look slightly stretched if popup isn't ~16:9 (cosmetic).
- In flight: none — all changes deployed.

## Next
- Owner: fix the Ctrl+5/Ctrl+4/Ctrl+3 hotbar-paging conflict (or tell me new keys).
- Reconfirm SPICE stuck-Alt is gone after a real Alt+Tab cycle in the popup.
- Timed buff macro (use_ability sequences + scheduler) still pending.

## Owner input needed
- Hail/sow/item_use keys (resolve the Ctrl+# hotbar-page collision).
- Set the `camp` key in the keymap (Stop Bot uses it).

## Commit(s)
- (this commit)
