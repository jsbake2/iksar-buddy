# Session Log — 2026-06-14

## Scope this session
Replace the flaky LaunchPad→char-select-OCR-click login (loaded the wrong toon when
the list order changed; fought an unreliable game-window click offset) with a robust
**direct login** + **same-account `/camp` switch**, wired into the forge Launch button.
Plus: grow each VM's C: by 20GB (in-game low-disk warning).

## Changed
- `forge/login.py` (NEW) — `LoginDriver`: full hands-off login flow + `camp_to()`.
  Host drives phases, gates on screenshots (OCR/pixel). AHK snippets fired via `ibrun`.
- `forge/guest.py` — added `run_ahk(script)`: base64-ships an AHK v2 script to
  `C:\ib\launcher.ahk` and fires `ibrun` (interactive session 1). Single substrate
  for the whole login/switch flow.
- `forge/controller.py` — `_launch` now calls `LoginDriver.login` (direct login to the
  dropdown character) instead of the launcher.ahk char-select + OCR pick. If EQ2 is
  already up it `/camp`-switches. `switch_char` wired to `camp_to`. Loads
  `accounts.yaml` (creds by VM domain) from the owner data dir. Removed `_select_character`.
- `config/forge/accounts.example.yaml` (NEW) + `.gitignore` — creds live in
  `IB_FORGE_DIR/accounts.yaml` (gitignored), keyed by VM dom; deployed to server.

## Decisions made
- **Direct login over char-select.** char-select OCR was unreliable (changing list
  order, duplicate names across servers, click offset). Logging straight into the
  named character via the EQ2 login form sidesteps all of it. Owner agreed.
- **`SetKeyDelay 55, 45` in Event-mode `{Raw}`** — Event mode raced the Shift key and
  corrupted password symbols (`3`→`#`, `$`→`4`, still 13 chars so the dot-count looked
  right). The delay lets Shift settle. THE key fix; without it login is rejected.
- **EQ2 dialog/button clicks must use `Guest.click` (ibgclick)**, never AHK `Click()`
  (EQ2 ignores it). First-login "Import UI Settings" dialog dismisses with `{Enter}`.
- **Host-orchestrated, not one monolithic AHK** — detection/gating in Python where it's
  debuggable; each guest action a tiny AHK. LaunchPad PLAY button is a stable fixed
  pixel (owner-confirmed); detect orange face at (1300,752).
- **Disk grow:** live `virsh blockresize` + online `Resize-Partition` for the running
  VM; `qemu-img resize +20G` + boot/extend/shutdown for the off ones. C: is the last
  partition on all three (no trailing recovery) so it extends cleanly.

## Changed (healer — second pass)
- `brain/charswitch.py` — rewritten to use `LoginDriver`: `healer_login` (boot + direct
  login to active-profile char) and `healer_switch` (`/camp`). Dropped old OCR char-pick.
- `brain/web/app.py` — `/api/launch` → `healer_login`; `/api/profile/{name}/swap` →
  `healer_switch`. No more `launch_bot.sh` + `select_only`/`camp_and_select`.
- `forge/login.py` — added `load_accounts()` + `LoginDriver.boot_and_login()` (shared
  cold path). **World-field fix:** `_login_ahk` now fills World too (healer's form had
  World blank → login dropped to char-select; vm1/vm3 had Wuoshi pre-filled so it was
  hidden). `_await_world` now detects char-select (`_char_select_present`) so it no
  longer false-reports in-world. UI-import dismiss = ibgclick Cancel (focus) + Enter
  (activate) — click-alone / Enter-alone are both unreliable on that modal.

## State now
- **Working (proven live, via deployed code):**
  - Direct login power-off→in-world: vm3 → Croolst, healer/iksar_buddy → Croolst (both
    through the driver); vm1 → Foxyman (earlier).
  - `/camp <name>` switch: Foxyman → Cruelst on vm1.
  - Healer is on the SAME path as forge now (`healer_login`/`healer_switch`).
  - Disk: all three C: now ~99.7GB (healer 33.8 / vm1 29.2 / vm3 29.5 GB free).
  - ib-forge + ib-brain deployed + restarted, active.
- **Found:** healer account = **robskin2004** (same account as crafter vm2/iksar_buddy3
  — that's why the interlock exists; they can't both be logged in). Creds now in
  accounts.yaml. Healer active profile = croolst → logs in Croolst.
- **Broken / known issues:**
  - LaunchPad **forgotten-creds** branch (`_launchpad_login_needed` + EULA) coded but
    UNTESTED — all accounts auto-login. Coords best-guess until a creds-forgotten case.
  - `boot_and_login` edge case: if EQ2 is already up but parked at CHAR-SELECT (not in
    world), it tries `camp_to` (chat /camp) which won't work there. Rare; cold boot and
    in-world switch are the common paths. Quit EQ2 first if stuck.

## Next
1. Owner end-to-end test from the dashboards: Launch (cold) + switch on healer + crafters.
2. Validate the LaunchPad forgotten-creds branch whenever it actually appears.
3. Junk the now-unused `infra/vm/ahk/launcher*.ahk` char-select path + `launch_bot.sh`.

## Owner input needed
- (none blocking) — healer creds found + set.

## Commit(s)
- (this commit) — direct login + /camp switch driver, wired into forge; VM C: +20GB.
