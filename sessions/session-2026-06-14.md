# Session Log — 2026-06-14

## Scope this session
Replace the flaky LaunchPad→char-select-OCR-click login (loaded the wrong toon when
the list order changed; fought an unreliable game-window click offset) with a robust
**direct login** + **same-account `/camp` switch**, wired into the forge Launch button.
Plus: grow each VM's C: by 20GB (in-game low-disk warning).

## Changed
- `forge/login.py` (NEW) — `LoginDriver`: full hands-off login flow + `camp_to()`.
  Host drives phases, gates on screenshots (OCR/pixel). AHK snippets fired via `ibrun`.
- `forge/guest.py` — added `run_ahk(script)`: base64-ships an AHK v2 script to
  `C:\ib\launcher.ahk` and fires `ibrun` (interactive session 1). Single substrate
  for the whole login/switch flow.
- `forge/controller.py` — `_launch` now calls `LoginDriver.login` (direct login to the
  dropdown character) instead of the launcher.ahk char-select + OCR pick. If EQ2 is
  already up it `/camp`-switches. `switch_char` wired to `camp_to`. Loads
  `accounts.yaml` (creds by VM domain) from the owner data dir. Removed `_select_character`.
- `config/forge/accounts.example.yaml` (NEW) + `.gitignore` — creds live in
  `IB_FORGE_DIR/accounts.yaml` (gitignored), keyed by VM dom; deployed to server.

## Decisions made
- **Direct login over char-select.** char-select OCR was unreliable (changing list
  order, duplicate names across servers, click offset). Logging straight into the
  named character via the EQ2 login form sidesteps all of it. Owner agreed.
- **`SetKeyDelay 55, 45` in Event-mode `{Raw}`** — Event mode raced the Shift key and
  corrupted password symbols (`3`→`#`, `$`→`4`, still 13 chars so the dot-count looked
  right). The delay lets Shift settle. THE key fix; without it login is rejected.
- **EQ2 dialog/button clicks must use `Guest.click` (ibgclick)**, never AHK `Click()`
  (EQ2 ignores it). First-login "Import UI Settings" dialog dismisses with `{Enter}`.
- **Host-orchestrated, not one monolithic AHK** — detection/gating in Python where it's
  debuggable; each guest action a tiny AHK. LaunchPad PLAY button is a stable fixed
  pixel (owner-confirmed); detect orange face at (1300,752).
- **Disk grow:** live `virsh blockresize` + online `Resize-Partition` for the running
  VM; `qemu-img resize +20G` + boot/extend/shutdown for the off ones. C: is the last
  partition on all three (no trailing recovery) so it extends cleanly.

## Changed (healer — second pass)
- `brain/charswitch.py` — rewritten to use `LoginDriver`: `healer_login` (boot + direct
  login to active-profile char) and `healer_switch` (`/camp`). Dropped old OCR char-pick.
- `brain/web/app.py` — `/api/launch` → `healer_login`; `/api/profile/{name}/swap` →
  `healer_switch`. No more `launch_bot.sh` + `select_only`/`camp_and_select`.
- `forge/login.py` — added `load_accounts()` + `LoginDriver.boot_and_login()` (shared
  cold path). **World-field fix:** `_login_ahk` now fills World too (healer's form had
  World blank → login dropped to char-select; vm1/vm3 had Wuoshi pre-filled so it was
  hidden). `_await_world` now detects char-select (`_char_select_present`) so it no
  longer false-reports in-world. UI-import dismiss = ibgclick Cancel (focus) + Enter
  (activate) — click-alone / Enter-alone are both unreliable on that modal.

## State now
- **Working (proven live, via deployed code):**
  - Direct login power-off→in-world: vm3 → Croolst, healer/iksar_buddy → Croolst (both
    through the driver); vm1 → Foxyman (earlier).
  - `/camp <name>` switch: Foxyman → Cruelst on vm1.
  - Healer is on the SAME path as forge now (`healer_login`/`healer_switch`).
  - Disk: all three C: now ~99.7GB (healer 33.8 / vm1 29.2 / vm3 29.5 GB free).
  - ib-forge + ib-brain deployed + restarted, active.
- **Found:** healer account = **robskin2004** (same account as crafter vm2/iksar_buddy3
  — that's why the interlock exists; they can't both be logged in). Creds now in
  accounts.yaml. Healer active profile = croolst → logs in Croolst.
- **Broken / known issues:**
  - LaunchPad **forgotten-creds** branch (`_launchpad_login_needed` + EULA) coded but
    UNTESTED — all accounts auto-login. Coords best-guess until a creds-forgotten case.
  - `boot_and_login` edge case: if EQ2 is already up but parked at CHAR-SELECT (not in
    world), it tries `camp_to` (chat /camp) which won't work there. Rare; cold boot and
    in-world switch are the common paths. Quit EQ2 first if stuck.

## Guest hardening (third pass)
- `infra/vm/harden-guest.ps1` (NEW) — applied to all three guests via the qemu agent
  (runs as SYSTEM). Disables: Windows Update (NoAutoUpdate + NoAutoRebootWithLoggedOnUsers
  + wuauserv/UsoSvc/WaaSMedicSvc/DoSvc/BITS disabled + UpdateOrchestrator/WindowsUpdate
  scheduled tasks disabled), telemetry (DiagTrack/dmwappushservice + AllowTelemetry=0,
  CEIP/AppExperience/Feedback tasks), Game Bar/DVR (AllowGameDVR=0 + per-user
  GameConfigStore + Xbox services), Store auto-download, consumer features, Delivery
  Optimization, Windows Error Reporting; sets power to never sleep/blank/spin-down + no
  hibernate. Idempotent; reports service states + policy values.
- **Why:** the System event log showed Event 19 (update installed) ×2 + wininit-initiated
  power-offs (1074) today — Windows Update was rebooting/powering off the VMs mid-session
  (it killed the in-world healer + vm1 while idle, and earlier interrupted a login).

## Next
1. Owner end-to-end test from the dashboards: Launch (cold) + switch on healer + crafters.
2. Validate the LaunchPad forgotten-creds branch whenever it actually appears.
3. Junk the now-unused `infra/vm/ahk/launcher*.ahk` char-select path + `launch_bot.sh`.
4. Consider a reboot of each VM at a quiet time to confirm WU stays disabled across boot
   (registry Start=Disabled persists; WaaSMedic task is disabled — should hold).

## Owner input needed
- (none blocking) — healer creds found + set.

## Commit(s)
- (this commit) — direct login + /camp switch driver, wired into forge; VM C: +20GB.

---

# Session 2 (evening) — Crafting calibration from owner screenshots + worker wiring

## Scope
First real crafting calibration. Pulled live screenshots off iksar_buddy2, owner
marked them up (bold opaque fills over each target; fill color + filename = the
element). Diffed marked-vs-clean to recover exact regions, confirmed the reaction
mechanic with the owner, filled `craft.yaml`, and wired the worker to the confirmed
rules. Single-craft slice ready to test (recipe: **rawhide leather backpack**, tailor).

## Intel gathered (claude_findings/FINDINGS.md + overlays)
- Pulled 9 EQ2 screenshots out of the running guest via the qemu agent
  (`guest-file-read`, no VM disruption) → `response/craft-vm1-screenshots/`.
- Built labeled overlays in `claude_findings/` (my boxes on the clean frames).

## Confirmed mechanic (owner = SME)
- Bottom bar mid-craft = `[book][1][2][3][4][5][6][stop]`. **1/2/3 = durability arts,
  4/5/6 = progress arts.** 3 reaction TYPES, paired **1↔4, 2↔5, 3↔6**.
- Capture icons **1/2/3 fresh each craft** as match refs. Active reaction shows in the
  **counter slot (~598,641)** next to its name (e.g. "Orange Hot Metal"); match → type
  N∈1..3 → press **N** if durability **<80%**, else **N+3**.
- Idle (no counter): cycle the active set (1/2/3 or 4/5/6) with ~0.3s pauses, break the
  instant a counter appears.
- **Durability <80% → durability arts; ≥80% → progress arts.** Top green bar = durability.
- **Power <50% → pause (single craft) / barrel forward (writs).** Reuse healer power bar.
- **Begin = RIGHT button (784,707)** (owner's filename was transposed — clean text wins);
  Create = LEFT (492,707), kept as a fallback var. Retry shares Begin's spot on reruns.

## Calibrated coords (craft.yaml, 1920×1080)
- durability_mode pixel (824,262) green=(34,205,46); power (74,46) blue; begin/retry
  (784,707); reaction button_regions 1/2/3 at x619/662/705 y689 w42h42; counter region
  (578,621,40,40); recipe_select clear(445,180)/search(349,180)/result(377,272)/
  focus(837,144); craft_focus (837,144); journal_ocr (1562,239,347,367).

## Code changes
- `forge/sensors.py` — added `game_present()` (self power-bar blue ⇒ in-world); folded
  it into `chat_safe()` as a hard precondition (fail-closed: never inject unless in-world
  AND chat input clear).
- `forge/worker.py` — **dropped the post-Begin ENTER** (in-world Enter opens chat = leak;
  the click alone starts the craft). Writs (`gate_power=False`) skip the low-mana pause +
  between-craft mana recover (barrel forward).
- `config/forge/craft.yaml` — real calibration (above) + `game_present` block; chat_input
  region kept (dark when chat closed → no false-block).
- `config/forge/lists.yaml` — `leather-bags-test` = rawhide leather backpack ×5.
- Tests: 37 pass.

## TUNE live (flagged, not blocking)
- **Begin/Retry fingerprint** (gold @784,707) is the craft heartbeat — verify it doesn't
  false-hit; a false hit ends a craft early, a miss just hangs (both chat-safe).
- **Durability 80% sample** sits next to a gold quality tick — nudged to x824; confirm.
- **clear-X (445,180)** — near the search dropdown; verify it clears, doesn't open the ▼.
- **chat_input line** — confirm it renders at (50,1019) with a live chat-open frame.

## Next
1. Deploy forge/ + config/forge/ to server (~/ib-app); restart ib-forge (ASK first — it
   bounces the forge dashboard).
2. Live single-craft test: Launch vm1 → Robskin, Start single "rawhide leather backpack".
   Tune the flagged fingerprints from what we see.

## Owner input needed
- Go-ahead to restart ib-forge after deploy.
