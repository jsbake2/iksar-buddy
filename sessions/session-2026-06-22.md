# Session Log — 2026-06-22

## Scope
Stand up the **harvesting bot** (new tool) — client-memory + navigation, 100% client-side.
Plus: STOP/log-completion/etc. crafter fixes deployed earlier in the day (see commits).

## Big results
- **Client memory reading PROVEN** on the live EQ2 client (pymem, in-guest). Read MZ header,
  ~3 GB scannable.
- **Reliable player position SOLVED**: `[EverQuest2.exe + 0x1822b68]` = float32 X,Y,Z,
  module-static (stable across move/session/restart; only client updates shift it). Validated
  tracking all move directions. Heap actor addresses RELOCATE — unused. (See
  [[harvest-memory-re]].)
- **Re-calibrate** path for client updates: `/loc` seed → scan module range for the triplet.
- **harvest.jsb-emr.us** live + Cloudflare Access-gated (Trusted Users), DNS+ingress wired,
  app serving on :18082. New CF API token works (account/tokens/verify endpoint).
- **Harvest dashboard MVP built + deployed** (harvest/ module): live position via in-guest
  reader → host poller → /api/state → dashboard. 7-point layout + real-time monitor, manual
  controls (move/strafe/turn/stop/jump/harvest/unstuck), route record (start/stop, samples on
  distance-or-time, closes loop, per-zone storage), char swap (multi-account login), console
  link, recalibrate button.
- **HARVEST.md** — full design/spec for all 10 owner tasks incl. movement smoothing, route
  tool, alerts, aggro avoidance, ?-nodes, opsec pause-when-players-nearby (~30 m).

## Wall (documented, not failed)
- **Spawn/actor list** (harvestables/monsters/players/zone-count/aggro/?-nodes DATA): the
  player actor is a heap object that relocates; its inline position diverges from the
  module-static "camera" copy when moving, so the slow guest-exec scan re-find is fragile.
  This is an interactive freeze + pointer-scan job (Cheat Engine). Dashboard panels for these
  are scaffolded "RE pending".
- **Cheat Engine** install: download 404s (CE site uses a download page, not a direct URL).
  Not blocking — pymem has done everything so far. Revisit (manual grab via console, or find
  the real URL).

## Architecture (new tool, mirrors forge)
- Runs on the **GPU VM iksar_buddy** (open-world rendering). Login: meatwad33w →
  **Furyflatulence** via the existing LoginDriver with EXPLICIT creds (multi-account — the
  driver already supported it; only controllers dom-keyed). Account interlock: meatwad33w
  (account1) shared with the iksar_buddy2 crafter.
- `harvest/` : __main__ (FastAPI :18082, title `ibh`, poller, route record, manual move,
  recalibrate, char swap), memory_read.py (in-guest pymem reader; position works, spawns
  null pending RE), web/index.html (dashboard).

## Owner ideas captured (memory)
- [[memory-reading-for-bots]] — reuse memory reading for HEALER HP/mana (biggest win, kills
  the ~1.4 Hz pixel cap) + crafter counter detection.
- [[client-side-only-rule]] — HARD: never server-side.

## Next
- Finish the **spawn list** (Cheat Engine: freeze position, pointer-scan to static base, "find
  what accesses" → spawn manager; map a per-entry type field). Unlocks tasks 1–5 & 10.
- Heading (module-static, same trick). Autonomous nav loop (follow route + skeleton-to-node +
  smoothing). Wire log-scrape alerts (combat/PM/rare/can't-harvest) + stuck detection.

## Commit(s)
- `<hash>` — harvest: new tool — client-memory player tracking + dashboard MVP + HARVEST.md spec
