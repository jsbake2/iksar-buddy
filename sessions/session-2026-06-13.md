# Session Log — 2026-06-13

## Scope this session
Kick off the **crafting bot** as a new tool. Read the old (dinosaur) craft_bot
code for its game-mechanic logic, study the live healer's architecture for the
plumbing, and write a plan doc to work from. No code yet — plan + recon.

## Done
- Read the dino crafting code in full: `~/from-windows/Documents/eq2/craft_bot/`
  (`craft.py` 1442L — the good one; `writ_crafter.py`; `config.json`) and the
  Downloads variant + `two_box/` 2-box orchestration. Extracted the working logic:
  Begin/Retry loop, `CounterWatcher` reaction-template matching, durability/progress
  mode pixel, power gate, writ OCR→select-recipe→craft-N.
- Re-read the live healer plumbing to copy: `shared/protocol.py`, `brain/server.py`,
  `agent/host_agent.py` + `host_sensor.py` (host-side `virsh screenshot`+`magick`
  sense, `keys.txt`+`ibkey` AHK inject, `gclick`/`gtype` primitives), `brain/config.py`,
  `iksar_buddy.xml`, key_ev.ahk.
- **Recon finding:** this box (`JASON-MAIN-CACHY`) is the **workstation**, not the live
  server. It has legacy VMs (`eq2-gm-vm` 51G = the dino, `razer-win10`, `satisfactory`),
  31G RAM (17G free), 298G disk. The live healer (`iksar_buddy` + 4070) runs on the
  server `10.0.0.16`. Saved to memory (machine-topology).
- Wrote **`FORGE.md`** — full spec + phased roadmap for the crafting tool.

## Decisions made
- **Codename Forge**: package `forge/`, process `ibf`, dashboard `:18081` (18080=healer,
  8080=taken on server). No `eq2`/`bot` in names (opsec).
- **Keep the dino's LOGIC, burn its skin.** Per owner: old code is bones to study; all
  input/connection/config/UI comes from the healer (host-side virsh/magick/AHK/gclick).
- **Drop the brain↔agent socket** for Forge — it's vestigial in the healer (both halves
  are host-side over localhost now). One asyncio process: N `CraftWorker`s + dashboard.
  `Guest(dom,w,h)` = host-helpers generalized to a libvirt domain (the reusable core).
- **Crafting needs no GPU** → clone runs GPU-less software render; frees the 4070 for AI.
- **Recommend running craft guests on the workstation** (spare RAM, keeps server intact).

## State now
- `FORGE.md` committed. No code written yet. Healer untouched (27 tests still pass).
- Known gotcha to fix in Phase 0: `host-helpers/gclick.py` hardcodes `W,H=1024,768`;
  clicks on a 1920×1080 guest land wrong. Forge is click-heavy → must scale by true res.

## Next
- Owner decides §12: where craft guests run (workstation vs server), GPU-less OK,
  resource budget, clone go-ahead + 2nd EQ2 login creds.
- Then: scaffold `forge/` + `Guest` core, fix gclick scaling, round-trip test, clone
  `iksar_buddy2`, calibrate one guest, land the Phase-1 single-craft slice.

## Owner input needed (FORGE.md §12)
- Craft guests on workstation or server? GPU-less profile OK?
- Clone `iksar_buddy` → `iksar_buddy2` + set up 2nd EQ2 login creds.
- Craft art keybinds (progress arts / durability arts / which counter art per reaction).
- Trade classes to support first.

## Commit(s)
- FORGE.md + this session log.
