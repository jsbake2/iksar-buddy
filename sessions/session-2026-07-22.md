# Session Log — 2026-07-22

## Scope this session
Add a resurrect capability: the bot must detect a group member has died and rez
them immediately (and make sure rez is a mappable keymap button).

## Changed
- `brain/policy.py` — NEW auto-rez block in `decide()`. When a present member is
  `dead` and no LIVING member is in emergency and we aren't mid-cast, emit a `rez`
  action targeting the downed member. Target order = `rez_priority` (role list,
  tank-first tiebreak). Placed right after the emergency-heal block and BEFORE the
  cures/heals block, so rez ranks near the top of the priority burst (outranks
  routine heals/cures) but still yields to a living emergency. Gated on
  `auto_rez` threshold (default True).
- `brain/server.py` — added `"rez": 10.0` to `ACTION_COOLDOWN_S`. Rez is a ~6s
  locked cast + accept latency; the cooldown stops us stutter-recasting our own
  rez every sensor tick. Tunable via `thresholds.cooldowns_s.rez`.
- `config/thresholds.yaml` — added `auto_rez: true` and `cooldowns_s.rez: 10.0`.
- `config/profiles/{croolst,croalst,furyflatulence,jenskin}.yaml` — flipped `rez`
  `mode: manual → auto` + refreshed desc (it's auto-cast now). Cosmetic only:
  `mode` is a keymap-UI hint, not consumed by the policy.
- `tests/test_policy.py` — 7 new tests (fires on death, tank-first priority,
  living-emergency blocks rez, outranks routine heal, fires OOC, disable flag,
  no-rez-when-nobody-dead). Full suite 113 passed.
- **Focus overlay button-size slider** (owner: buttons too big over the game
  window). `focus.html` — added a `size` range slider (0.55–1.3) to the ⚙ editor
  actions. `focus.js` — `layout.scale` (persisted per kind, server+localStorage
  like cols/colors) drives a `--fscale` CSS var on `.focus-grid`; `applyScale()`
  paints the var + syncs the control without a button rebuild (smooth drag), saves
  on release. `themes.css` — `.fbtn`/`.fbtn.big`/grid gap+padding now `calc(px *
  var(--fscale))`; `.ed-scale` slider styling. Bumped focus cache `?v=20260722a` +
  `FOCUS_BUILD b12`. Deployed to live `~/ib-app/brain/web/static/`.

- **Dirge single-target backup heal** (owner: the Dirge will have a single-target
  heal that needs a keymap + auto-cast). `config/profiles/joar.yaml` — new `st_heal`
  ability (auto, blank key for the owner to fill). `brain/server.py` — new
  `_hb_dirge_heal` heartbeat spec: tops off the lowest LIVING member below
  `dirge_heal_hp` (tank-first), in/out of combat, per-target `dirge_heal_recast_s`
  cooldown; runs FIRST in the heartbeat tuple. `config/thresholds.yaml` —
  `dirge_heal_hp: 0.70` + `dirge_heal_recast_s: 2.5`. `focus.js` — DIRGE_CATALOG
  "Heal Tank" button (`st_heal`, targets tank) + added to dirge ENSURE so existing
  layouts get it. `tests/test_dirge_heal.py` — 7 tests. Deployed live (code to
  `~/ib-app`, `st_heal`+thresholds injected surgically into `~/ib-data/config`).

- **Forge recipe-picker fixes (Sage 'Master of the Hunt II (Journeyman)')**
  `forge/sensors.py` — three linked bugs: (1) a `(Quality)` that OCRs on its OWN line
  at full row-pitch was left as a separate row (`_merge_quality_tails` folds it up +
  forces `wrapped`); (2) the wrong-quality reject substring-matched "master" IN the
  recipe NAME → derive `target_q` from the trailing parenthetical only and exclude
  name-token qualities; (3) a row's own `(Quality)` tag counted as a surplus word when
  the target omitted it → `qfree`. `tests/test_forge.py` +2 tests. Deployed live.
- **Per-bot OCR debug capture** (owner: keep a log + screenshots each OCR grab, roll
  off at ~200, toggle per bot on the forge page). NEW `forge/debug.py` — capture()
  copies the guest frame to a timestamped PNG + a log line pointing to it, ring-buffered
  at 200/bot, toggle persisted to enabled.json. Hooked into `worker._select_recipe` on
  recipe-match AND recipe-miss. `forge/web/app.py` — GET/POST `/api/bot/<id>/debug` +
  `/debug/shot/<name>` (path-traversal safe). UI: 🐞 Debug toggle + captures drawer
  (newest-first, click a row to open the frame). Deployed live + `ib-forge` restarted.

## Decisions made
- **The whole execution path already existed** — the keymap already renders `rez`
  as a mappable ability (Ctrl+= live / Ctrl+1 repo), and the agent already does
  target-then-cast (presses the slot's F-key, then the ability key) for any
  COMMAND with a `target_slot`. `rez_priority`, `REZ_LOOP`, `FORCE_REZ` were all
  defined but **vestigial** — nothing in `decide()` ever emitted a rez. So the
  only real gap was the decision. Implemented that; reused `rez_priority`.
- **Live-emergency guard, not combat-gated.** Owner said "rez immediately," so rez
  fires in ANY state (combat / wipe-recovery / OOC) — EXCEPT while a living member
  is below the emergency HP line (a locked 6s rez over a dying teammate wipes the
  group; heal them first, rez slots into the next gap). Also skips mid-cast so it
  doesn't interrupt an in-flight heal.
- **Did NOT touch live profile keybinds.** Live fury (`~/ib-data/config/profiles/
  furyflatulence.yaml`) has `rez: Ctrl+=` (repo says Ctrl+1) — classic profile
  drift; auto-rez uses whatever key is live, so no need to overwrite. Only the
  additive `auto_rez` knob was injected into live thresholds.

- **Dirge heal is a DISTINCT role (`st_heal`), not `direct_heal`/`heal`.** Two
  reasons: (1) `heal`/`direct_heal` are in `_MEMBER_ACTIONS`/the healer `decide()`
  stack — reusing them would double-fire the dirge heal through the healer priority
  loop at the healer's aggressive 0.90 threshold; (2) a separate role gets its own
  `dirge_heal_hp` threshold so a support toon tops off only when meaningfully hurt
  (0.70) instead of proactively at 0.90. The heartbeat spec is role-gated (empty
  `key_for('st_heal')` on healer profiles → no-op), so it's dirge-only automatically.

## State now
- Working: auto-rez decision + tests (113 pass). DEPLOYED to live `~/ib-app/brain/`
  (policy.py + server.py, byte-compiled clean) and `auto_rez: true` added to live
  `~/ib-data/config/thresholds.yaml`. Live brain was NOT running and healer VM is
  shut off (from 2026-07-14), so deploy disrupted nothing.
- Broken / known issues: none known. Rez is a fire-and-forget cast — we can't sense
  the cast bar, so anti-spam relies on the 10s cooldown, not cast-completion.
- In flight: none.

## Next
- Live smoke test whenever the owner next runs the healer: kill a grouped member,
  confirm the bot targets (F-key) + casts rez (Ctrl+=) within a tick, and that the
  10s cooldown prevents re-spam. Confirm the human tank gets the revive prompt.
- If the owner wants rez held until OOC (safer, less "immediate"), add a
  `rez_in_combat: false` gate — trivial, not built since he asked for immediate.

## Owner input needed
- ~~SME check: Fury rez castable in combat?~~ **CONFIRMED by owner: Fury is a
  combat rez.** No combat-gate needed — the immediate-fire design is correct as-is.
- Confirm `rez_priority` order (currently tank→healer→support→dps) matches intent.

## Commit(s)
- (this commit) — auto-rez on group death
