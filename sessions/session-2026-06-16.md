# Session Log — 2026-06-16

## Scope this session
Two threads: (1) build the recipe-data gathering pipeline (EQ2U scrape → per-class/level
lists for the crafter); (2) fix a live crafter bug where recipe search thrashed on
variant collisions (Imbued/Blessed).

## Changed
- `forge/sensors.py` — `match_recipe_row`: reject result rows carrying a variant
  modifier (Imbued/Blessed, config `recipe_select.variant_modifiers`) the target name
  lacks, and tiebreak toward the EXACT name (fewest surplus letters). Fixes the wrong-row
  load / clear-and-retry thrash when "Imbued Iron Chainmail Coat" token-covers
  "Iron Chainmail Coat" 1:1.
- `tests/test_forge.py` — 3 tests for the above (reject imbued, prefer exact over suffix,
  allow imbued when target IS imbued). Mocks `_ocr_words`.
- `tools/recipe_scrape/` (NEW, untracked until data lands) — Playwright EQ2U scraper.
  `recon.py` (one-shot DOM dump), `scrape.py` (login → master book index → per-book
  recipe fetch → per-class/level JSON + flat CSV). `.gitignore` updated to exclude the
  run logs (carry session cookies), raw page cache, and recon dumps.

## Decisions made
- Recipe source = EQ2U/EQ2wire, not DBG Census. Confirmed (again) Census `recipe`
  collection has only name/bench/id — no level/tier/class, no recipebook collection.
  EQ2U has level+class but is behind login + Cloudflare → Playwright required.
- Scrape mechanism: Playwright logs in ONCE to clear Cloudflare, then bulk-fetch book
  pages via the authenticated request context (no per-page render). Server returns empty
  200s under sustained rate → hardened fetcher treats short/empty bodies as failures and
  RE-LOGINS (fresh ci_session + __cf_bm) + proactive re-login every 400 fetches. Raw HTML
  cached per book → resume-friendly.
- Classifier (book name → class): Advanced<Class>/archetype-essentials → class(es);
  artisan/crafter → generic "Artisan" (L1-9 + expansion all-class books the owner asked
  for); adorner/tinkerer in name → side classes; mastercrafted one-offs (Ancient
  Knowledge / Draconic Knowledge / Ancient Teachings) have no class in name → "Misc".
- Output layout (owner picked "Both"): canonical flat data (CSV + by_class JSON) stays
  denormalized for the bot; browse UI = class-first tier tree default + global-search flat
  table. UI deferred until data lands (schema is locked: class/level/category/book/recipe).
- Tinkerer/Adorner are skill-based (0-350 @ L70), not char-level → routed to `data/side/`
  keyed by book, not level. Precise 0-350 skill number is NOT on the recipe page text;
  capturing it needs a per-recipe detail pass (~20k fetches) → deferred as optional.

## State now
- Working: crafter variant-disambiguation fix (13/13 forge tests pass). Scraper logs in,
  passes Cloudflare, fetches reliably with re-login recovery.
- In flight: full scrape running in background (PID 1490406, run2.log) — ~2375/3730 books
  at last check. On completion, RE-RUN `scrape.py` (cache hit → instant) to regenerate
  outputs with the improved classifier, then validate counts + commit data.
- Known issue (resolved this session): first scrape run cached 2539 empty 200s as 0-byte
  files; hardened fetcher + deleted empties + resumed.

## Next
1. Scrape finishes → re-aggregate from cache → eyeball per-class counts, filter obvious
   junk ("Test Only ..." dev books) → commit `tools/recipe_scrape/` + `data/`.
2. Build recipe browser into the dashboard: class-first tier tree + flat filterable table.
3. Optional: per-recipe detail pass to attach the 0-350 skill number for tinker/adorn.
4. Crafter: owner restarts the crafter to pick up the sensors.py fix; retest Iron
   Chainmail Coat (and any imbued/blessed-adjacent recipes).

## Owner input needed
- Restart the crafter process to load the `match_recipe_row` fix, then confirm Iron
  Chainmail Coat crafts.
- Whether to fold TS Quest one-off recipes into the dataset (currently excluded).
- Whether the 0-350 tinker/adorn skill number is worth the extra detail-page pass.

## Crafter follow-up — "1 obvious result but it moves on / never clicks" (vm1)
Owner hit single-result recipes (Iron Revenant Mantle, Iron Vanguard Sabatons) being
DISMISSED ("not found") + Iron Chainmail Leggings (has an imbued variant).

Diagnosis via live screenshot of vm1 (`sudo virsh screenshot iksar_buddy2` on 10.0.0.16,
crafters stopped): replicated the EXACT OCR pipeline on the real frame — "Iron Vanguard
Sabatons" reads at conf 90+, scores 1.00, matches cleanly. **The matcher/OCR are sound.**
So the live failures are upstream + intermittent: (a) focus race where the atomic
focus+type+Enter doesn't land, list never filters, target isn't in the OCR'd rows; (b) the
imbued/blessed variant collision; (c) silent failure made it undebuggable. Also: the failing
recipes are NOT in lists.yaml (dashboard-saved list); "Iron Reverent Boots" in scribed-book-1
is likely a mis-scribe of "Revenant" — a wrong name => EQ2 search returns nothing.

Fixes (forge/sensors.py, forge/worker.py, +tests, 15/15 green):
- `worker._select_recipe`: after typing, verify `sensors.search_landed(query)` — if the
  query didn't reach the box (focus race), re-type immediately instead of burning the poll
  budget on an unfiltered list.
- On a failed match, push_log the OCR'd row blobs (`sensors.recipe_row_blobs`) — failures
  are no longer silent; the log shows wrong-name vs unfiltered vs OCR-miss.
- `match_recipe_row`: sole-result fast path (one row, partial coverage >= sole_result_floor
  0.34 => take it) so a lone garbled OCR isn't dismissed. Refactored row extraction into
  `_recipe_rows` shared by matcher + diagnostic.

Still pending owner restart (held until crafters finish lists) to validate live.

## Commit(s)
- `54202bd` — forge: reject Imbued/Blessed variant rows in recipe search; prefer exact
- (this commit) — forge: search-landed verify + sole-result fast path + non-silent match diag
