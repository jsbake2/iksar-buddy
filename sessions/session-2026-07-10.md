# Session Log — 2026-07-10 (Recast-buffs panel ported from Dirge → all healers; Fury HoTs)

## Scope this session
Owner: "implement all the changes we made for the Dirge into the healer" — specifically
give the healers the Dirge's periodic recast-buff machinery so a Fury can maintain named
HoTs (Warden-style). Named example: **Fury Flatulence**. Confirmed scope w/ owner:
port the Recast-buffs panel (named rows, auto-recast timers, live countdown, per-target)
to **all** healer profiles (Fury + Defiler + Inquisitor), used for HoTs.

## Key finding (why this was small)
The brain already ran `_hb_pbuffs`/`_hb_mana_feed` for EVERY profile — they're in the
`_run_heartbeats` tuple and gated purely by config (`pbuff_N_interval_s` + a mapped key),
NOT by profile kind. `/api/dirge/buffs`, `/api/dirge/buff/{n}/execute`, and the pbuff
tunables likewise read the active profile regardless of kind. The ONLY thing locking
healers out was the UI: the recast `<section>` carried `dirge-only` (hidden for healers)
and `pollRecast()` bailed on `profileKind !== "dirge"`. So this was mostly un-gating +
adding the pbuff roles to the healer profiles.

## Changed
- `config/profiles/croolst.yaml` (Fury) — added `pbuff_1..4` (mode auto, name field).
  `pbuff_1` seeded `name: Fury Flatulence`, desc "HoT (regrowth line)". These are named
  HoTs the owner maintains via the Recast panel, complementary to the existing single
  tank-HoT heartbeat (`hot`/`_hb_maint`).
- `config/profiles/jenskin.yaml` (Defiler) — added `pbuff_1..4` (auto, blank names).
- `config/profiles/croalst.yaml` (Inquisitor) — added `pbuff_1..4` (auto, blank names).
- `brain/web/static/index.html` — dropped `dirge-only` + `hidden` from `#recastPanel`
  so it renders for every profile kind. Comment updated (Dirge buffs AND healer HoTs).
- `brain/web/static/app.js` — removed the `profileKind !== "dirge"` guard in
  `pollRecast()`; updated the panel comment. `.dirge-only` toggling still governs the
  mana-feed tune row + `dirgeSections` (mana feed stays Dirge-only — bard power regen).
- `config/thresholds.yaml` — reworded the pbuff comment block (now HoTs + Dirge buffs).
- `brain/server.py` — `_hb_pbuffs` docstring: no longer "DIRGE"-only; serves HoTs too.

## Decisions made
- Kept the backend endpoint paths `/api/dirge/*` as-is (internal; renaming touches JS +
  risks live-code churn for zero user value). Clarified in comments that they serve any
  kind. If a future session wants tidy naming, rename to `/api/recast/*` in one pass.
- Recast panel is ALWAYS visible now (even with 0 pbuffs mapped) — rows render `.unset`,
  inviting the owner to set keys. Consistent with how the Dirge panel already behaved.
- pbuff intervals/targets live in the GLOBAL `thresholds.yaml` (shared across profiles,
  same as `ward_heartbeat_s`). Did NOT make them per-profile — out of scope; the keymap
  keys ARE per-profile so an unmapped pbuff simply won't fire.
- pbuffs recast IN_COMBAT only (unchanged `_hb_pbuffs` behavior) — fine for maintained
  HoTs; matches the single tank-HoT heartbeat.

## State now
- Working: 106/106 tests pass. Emulated `/api/dirge/buffs` against croolst → row 1 =
  "Fury Flatulence" (key_set False until owner maps a key). Keymap already renders a
  name input for any ability with a `name` field, so pbuff key+name are editable there.
- Broken / known issues: none known. UI not yet eyeballed live in a browser.
- In flight: none.

## Next
1. Owner: on the Fury (Croolst), open ⌨ keymap → set the key for **Fury Flatulence**
   (pbuff_1) + any other HoTs, then set interval/target on the Recast-buffs panel.
2. Live-verify in the dashboard that the panel shows + counts down for a healer profile.
3. Deploy to 10.0.0.16 (`~/ib-app`) when validated.

## Owner input needed
- In-game HoT keybinds + which HoT maps to which pbuff slot (SME call). Fury Flatulence
  is seeded as pbuff_1; the rest are blank.

## Commit(s)
- (this commit) — recast-buffs panel ported Dirge → all healers; Fury Flatulence HoT
