# Session Log — 2026-07-06 (Session A: REFACTOR foundations)

> Executing REFACTOR.md (full-repo review, uploaded 2026-07-06) — all five suggested
> sessions A–E, owner authorized full write access, **no live testing available today**.
> This log covers Session A (P0 foundations). Later commits today append their own logs.

## Scope this session

REFACTOR.md Session A: P0.1 promote forge/guest.py → shared/guest.py and port every
hand-rolled virsh/guest-exec copy onto it; P0.4 ONE offsets module (incl. the stale-offset
live bug); P0.6 delete duplicate files; P0.7 packaging fix. Plus P0.7's "consider":
LoginDriver → shared/ to break the backwards brain→forge dependency.

## Changed

- **shared/guest.py (NEW)** — Guest class moved from forge/guest.py, grown into THE
  host-side guest-I/O core: added `agent_cmd` (retrying qemu-agent-command, host_agent's
  proven pattern), `file_write` (guest-file API fast path, used by healer inject),
  `push_bytes/push_file/push_text` (chunked-b64 large-payload push), `read_ps` (hot-path
  exec_ps), `exec_start` (fire-and-forget), `exec_out` (any-binary with out+err capture,
  harvest's `_gx` pattern). All prior forge methods kept verbatim.
- **shared/login.py (git mv from forge/login.py)** — brain/charswitch importing from
  forge was backwards; login is shared infrastructure (brain, forge, harvest all log in).
- **forge/guest.py, forge/login.py** — one-line re-export shims (keep for one release).
- **forge/controller.py, forge/worker.py, forge/sensors.py, brain/charswitch.py** —
  imports moved to shared.guest / shared.login; stale comments updated (also brain/web/app.py).
- **guest_agent/offsets.py (NEW)** — the ONE offsets module (POS_OFF 0x1822b78,
  HDG_OFF 0x1822b84, ZONE_PTR, NODE_LO/HI, ACTOR_VT, NODE_CLASSES, vt-range consts, PROC).
  Docstring records the 2026-06-23 recal and the deploy path (pushed flat to C:\ib\agent\).
- **guest_agent/__init__.py (NEW)** — package marker documenting the dual import pattern:
  in-guest files do `import offsets` (sibling), repo-side falls back to `guest_agent.offsets`.
- **harvest/sense_daemon.py** — ⚠ had STALE pre-recal offsets (0x1822b68/74) → would read
  garbage position. Now imports from the shared module. (Note: this file is currently
  referenced by nothing — orphan; kept for now, flagged for Session D/E review.)
- **harvest/spawns_live.py** — SAME stale-offset bug (not flagged by REFACTOR.md; found
  during the port). Now imports offsets module.
- **harvest/sense_push.py, harvest/memory_read.py, guest_agent/harvest_agent.py** —
  inline offsets replaced with the shared import.
- **harvest/__main__.py** — ported onto shared.guest (removed sys.path hack, local VIRSH,
  hand-rolled `_gx`/`_push_text`). Added `_push_offsets()`; every deploy path
  (deploy_reader/deploy_spawns/start_sensor/deploy_agent) now pushes offsets.py so the
  in-guest import never fails. **deploy_agent was silently broken** — it pushed
  `harvest/_agent.py` and `harvest/hud_overlay.py` which don't exist; now pushes the real
  guest_agent/ files + nav_graph/sense_push/memory_read and logs missing sources.
- **agent/host_agent.py** — ported onto shared.guest: `_poll_gpu` via `exec_out`,
  `_guest_read` → `read_ps`, daemon deploy/inject via `file_write` + `agent_cmd`,
  PS-fallback inject via `exec_start` (now strips `'` from seq — P3.5 quote-injection fix),
  combat-log path PS-escaped (`'` → `''`).
- **harvest/re_tools/** — all six tools (fire_ahk, ibhwbp_push, calib, go_to_node,
  harvest_cycle, ibcap) rewritten onto shared.guest; each dropped its private
  agent()/gx()/push() plumbing. Behavior preserved exactly (calib's WinActivate-only
  focus, go_to_node's Click(960,540) FOCUS, harvest_cycle's log-offset tracking,
  ibcap's detached watcher + jitter). They now require the repo venv (editable install).
- **DELETED:** harvest/re_tools/spawns_live.py (md5-identical dup of harvest/spawns_live.py),
  agent/key_daemon.ahk (dup; canonical copy is infra/vm/ahk/key_daemon.ahk — host_agent's
  `_deploy_daemon` now reads from there).
- **pyproject.toml** — packages now include forge, forge.web, harvest, guest_agent (P0.7;
  `pip install -e .` previously didn't install forge/harvest at all).
- **tests/test_forge.py** — stale assertion fixed: sage `search_suffix` is "" since the
  owner's never-type-parens rule; test still expected `"Minor Healing (App"`.

## Decisions made

- LoginDriver moved to shared/ NOW (REFACTOR listed it as "consider") — brain importing
  from forge was the worst layering violation and the move was mechanical.
- forge/guest.py + forge/login.py kept as import shims for one release, then delete.
- infra/vm/host-helpers/gexec.py left as-is: it's a deliberately standalone scp-able CLI
  helper; porting it onto shared.guest would break its zero-dependency property.
- re_tools import shared.guest directly (no sys.path hack) — they're run from the repo
  checkout on 10.0.0.16 anyway; editable install is the supported way.
- exec_out returns "" on failure (matching the old `_gx`), agent_cmd raises — the
  distinction is deliberate: fire-and-read tools want soft-fail, inject wants loud-fail.

## State now

- Working: full test suite green (66 passed). `python -m compileall` clean; all key
  module imports verified. Nothing here is live-validated (owner unavailable today).
- Broken / known issues: none known introduced. Pre-existing: harvest/sense_daemon.py is
  an orphan (nothing references it); login res-mismatch (memory: login.py calibrated
  1024x768, harvest VM is 1920x1080) untouched.
- In flight: Sessions B–E of REFACTOR.md follow in subsequent commits today.

## Next

- Session B: heal_reflex one-grab-per-tick, vectorize guest_sense + capture.fill_ratio,
  non-blocking guarded_press, resolve `_DAEMON_ENABLED` flag/comment contradiction.
- Session C: P1 tunables → config + config/README.md; P3.1 server.py heartbeat table.
- Session D: split guest_agent/harvest_agent.py; P2.3 in-guest log tail; P2.5 login waits.
- Session E: web_common shared static, UI cleanup, docs (README/ARCHITECTURE/runbook),
  hygiene + ChatGuard/heal_ruleset tests.

## Owner input needed

- **Live validation pass** when available: (1) healer inject still lands (host_agent now
  drives Guest.file_write for keycmd.txt), (2) a forge craft cycle, (3) harvest
  deploy_agent — it never actually pushed the agent before, so first run pushes fresh
  code to C:\ib\agent\. All ports preserved wire behavior 1:1, but nothing has touched a
  live VM today.
- ~/ib-app on 10.0.0.16 needs a redeploy + `pip install -e .` (new shared/ + guest_agent/
  packages) — NOT done today per the no-interruption rule; healer/harvest are running.

## Commit(s)

- `d3707ea` — REFACTOR Session A: shared Guest core, ONE offsets module, dup deletes, packaging

---

# Session B — hot paths (P2.1/2.2/2.6/2.4)

## Changed

- **guest_agent/heal_reflex.py** — P2.1: was one `sct.grab(1x1)` per pixel (~15-25 mss
  round-trips/tick, the sense half of the tick). Now a `_Frame` class grabs the bounding
  rect over ALL ruleset locs (computed once in `__init__` from group/critical/cure/mana
  pixels) ONCE per tick; every read is an ndarray index. `_is/_group_size/_ailment` take
  the frame. `_safe()` unchanged (its two region grabs stay). No-locs ruleset → no-op tick.
- **agent/guest_sense.py** — P2.2: `_power_row_scan/_power_row/_fill/_detriments`
  vectorized (ndarray strip compares instead of ~10^4 per-pixel Python calls per frame).
  `_power_row` is now a thin wrapper over the scan. Semantics preserved exactly —
  including first-max tie-breaking and out-of-bounds-reads-as-black (see tests).
- **agent/capture.py** — P2.2: `fill_ratio` vectorized (one `np.all(|line-full|<=tol)`).
- **agent/client.py** — P2.6: `guarded_press` (sleeps in tap gap + abort/ESC path) now
  runs via `run_in_executor` from `_on_command`, so a blocked press can't stall the
  12 Hz sense loop.
- **agent/host_agent.py** — P2.4: the `_inject` comment claimed the daemon path was
  "DISABLED pending debug" while `_DAEMON_ENABLED = True`. The FLAG was right — the
  daemon shipped in bddf41f and got self-heal in 3797982. Comment now states the truth:
  daemon LIVE (~<0.1s), ibkey one-shot + PS are fallbacks.
- **tests/test_guest_sense.py (NEW)** — equivalence tests: the original per-pixel loops
  kept as reference implementations, vectorized versions must match them exactly on
  random-noise frames (incl. frame-edge clipping), plus planted-feature tests (blue
  power row, half-full bar, lit detriment cell). 8 tests.

## Decisions made

- heal_reflex chat-safety grabs NOT merged into the bounding rect — the chat input is at
  the bottom of the screen, so a union rect would be near-full-screen; 3 small grabs/tick
  beats 1 huge one.
- No performance measurement logged (REFACTOR wanted sense-ms before/after) — needs a
  live guest; flagged in Owner input.

## State now

- Working: suite green (74 passed, 8 new). heal_reflex/capture import mss/pydirectinput
  so they compile-check only on the host; guest_sense math is host-testable and tested.
- In flight: Sessions C–E follow.

## Owner input needed (appended)

- After redeploy: watch heal_reflex tick time in its log (expect the sense half to drop
  ~10x; tick can then be lowered from 0.1 toward 0.03–0.05), and confirm ib_agent's 12 Hz
  loop holds rate while a press is blocked (P2.6).

## Commit(s)

- `58186ce` — REFACTOR Session B: hot paths — single-grab heal tick, vectorized sensing, non-blocking press

---

# Session C — tunables → config (P1.1–1.7) + server heartbeat table (P3.1)

## Changed

- **config/thresholds.yaml** — new sections: command pacing (`gcd_s`, `cooldown_default_s`,
  per-action `cooldowns_s`, `prepull_debounce_s`) and event/combat detection
  (`rez_window_s`, `combat_hp_drop`, `combat_decay_s`, `chat_hysteresis_s`,
  `combat_log_poll_s`). All previously hardcoded in brain/server.py + agent/host_agent.py.
- **config/calibration.yaml** — new keys: `healer_dom` (P1.5 — THE VM domain, was a literal
  in 4 files), `eq2_log_template`, the LOAD-BEARING `sensor:` section (track ranges,
  pitch/slots/search, detriment cells, chat-input region, pixel thresholds
  blue_min_px/bright_sum/icon_sum/icon_frac) and `bar_colors:` (P1.3/P1.4).
- **config/harvest.yaml (NEW, P1.6)** — dom, spice_port, active_char, login-form coords
  (user_click/username_ocr), eq2_log_template, move_keys, sensor ingest_url/hz.
- **config/README.md (NEW, P1.7)** — the full knob table: file → key → default →
  consumer → effect, plus the hot-reload vs restart matrix and the IB_FORGE_DIR gotcha.
- **shared/tunables.py (NEW)** — one-shot YAML loader (IB_CONFIG_DIR, degrades to {}) for
  every non-brain consumer; brain keeps its own hot-reloading Config.
- **brain/server.py (P3.1 + P1.2)** — the ~90-line if/elif heartbeat ladder in
  `_on_state_event` is now a SPEC TABLE: `_run_heartbeats` walks
  (`_hb_maint`, `_hb_assist`, `_hb_debuff`, `_hb_mana_feed`, `_hb_pbuffs`), each returning
  None or (role, key, slot, reason, stamp-closure); first firable wins, GCD bumps, done.
  Member-row building and death-edge notify extracted (`_build_member_rows`,
  `_notify_death_edges`). GCD + per-action cooldowns + prepull debounce now read from
  thresholds.yaml via `_gcd()`/`_cooldown_for()` (constants = fallbacks). Behavior
  preserved: fixed `now` per event + deadline bump ⇒ still exactly one heartbeat/event,
  same maint→assist→debuff→mana→pbuffs order, mana feed still runs OOC.
- **agent/host_agent.py** — `_overlay_tunables()` at import: DOM/eq2_log_template from
  calibration.yaml, rez/combat/chat/log-poll knobs from thresholds.yaml.
- **agent/host_sensor.py** — `_overlay_calibration()` at import: healer_dom + the whole
  `sensor:` geometry (constants = fallbacks); magic literals 12/90/120/0.4 are now the
  named (and overridable) BLUE_MIN_PX/BRIGHT_SUM/ICON_SUM/ICON_FRAC.
- **agent/guest_sense.py** — `apply_calibration`/`apply_tuning`/`apply_names` mutate the
  module geometry/detection globals; same named constants as host_sensor.
- **agent/client.py** — CONFIG receipt now applies all three to guest_sense, so the
  in-guest agent follows calibration.yaml + thresholds.yaml + the active profile's names
  WITHOUT a guest redeploy.
- **brain/server.py push_config** — now also sends `thresholds` (was ability_map +
  calibration + names).
- **guest_agent/heal_ruleset.py (P1.4)** — `build_heal_ruleset(cal, am, thresholds=None)`:
  heal fracs from hp_standard/hp_critical/mana_floor, bar colors from
  calibration bar_colors, chat region from sensor.chat_input. CLI loads thresholds.yaml.
  ⚠ Behavior change on next ruleset regen: the reflex's mana gate moves 0.24 → 0.30
  (mana_floor). The old "ALIGNED to thresholds.yaml" comment was false — now it's true.
- **brain/web/app.py, brain/charswitch.py** — HEALER_DOM from calibration.yaml.
- **harvest/__main__.py** — DOM/SPICE_PORT/active_char/login coords/log path/move_keys/
  ingest from harvest.yaml; sensor deploy passes ingest URL+hz as argv.
- **harvest/sense_push.py** — HOST/HZ from argv (deployer-supplied) with baked-in fallback.

## Decisions made

- shared/tunables.py is ONE-SHOT (no hot reload) for non-brain consumers — they're
  restart-cheap daemons; the brain remains the only hot-reloader. The in-guest agent's
  "reload" is the CONFIG push (fires on connect + profile switch).
- heal_ruleset keeps 0.24 as the no-thresholds fallback but follows mana_floor when
  given thresholds — the discrepancy is now documented instead of hidden.
- IGNORE_SIGNATURES stays in code (host_sensor + guest_sense): it's an empty
  dict by design (rez sickness is contextual, not color-stable) — nothing to tune yet.

## State now

- Suite green (74 passed) + compileall clean + import/overlay smoke tests pass
  (host_sensor/host_agent/guest_sense pick up YAML values; heal_ruleset CLI emits the
  same JSON shape with config-sourced colors/fracs). NOT live-validated (owner away).
- In flight: Sessions D, E follow.

## Owner input needed (appended)

- The heal-reflex mana gate change (0.24 → 0.30) takes effect when the ruleset is next
  regenerated/pushed — lower `mana_floor` in thresholds.yaml if the old feel was right.
- Sensor ingest_url/hz changes need a "deploy sensor" from the harvest dashboard to land.

## Commit(s)

- (this commit) — REFACTOR Session C: every tunable into config/ + brain heartbeat spec table

---

# Session D (same day): harvest_agent split + in-guest combat-log tail + login waits

> REFACTOR.md Session D — P3.2 (split the 1470-line guest_agent/harvest_agent.py),
> P2.3 (in-guest combat-log tail; host PS poll becomes the fallback),
> P2.5 (shared/login.py blind sleeps → condition-or-timeout waits).
> Still no live testing available; pytest + compileall + AST cross-checks only.

## Changed

- **guest_agent/harvest_agent.py → six modules (P3.2), all code moved VERBATIM:**
  - `agentio.py` — TARGET/STATUS/HUD/STOP_FLAG paths, StopRequested, `_check_stop`,
    `_dbg`, `_status` (the never-raise atomic writers).
  - `win_input.py` — everything user32: `_win_pid`, `focus_eq2` (AttachThreadInput),
    `_eq2_pids`/`_eq2_window_any`, Event-mode taps/clicks/typists (`_tap`, `_click`,
    `_type_text`, `_combo`, `_clear_field`, `type_login_form`, `type_chat`), the guarded
    `_ctrl_chord` + `harvest_key`/`target_key`/`tab_key`, `_jump`, the `Keys` hold-state
    manager, pydirectinput PAUSE/FAILSAFE setup.
  - `eq2mem.py` — pymem plumbing: `_live_eq2`, `pm_open`, `state`, `_MBI`, heap scans
    (`scan_nodes`, `read_node_array`, `read_actors`, `node_addr_at`), all NODE_*/ACTOR_*
    constants, mob blacklist, `study_capture`, `_read_cstr`.
  - `nav.py` — GRAPH_FILE/ROAM + GRACE/FACE_TOL/etc, `_settle`, `signed_diff`, `nav`,
    `load_graph`, `reachable`, `_nav_unstuck`, `goto`.
  - `harvest_loops.py` — log dir/regexes (HARV/FAIL/RARE/CONSIDER/NOTARGET/
    NOT_ATTACKABLE/RE_DMG/FAR), `_combat_watch`, `_log_len`/`_log_since`,
    `_wait_harvest`, `harvest()`, `loop_main`, `_scan_thread`, `route_loop_main`,
    `_tour_anchors`, `gather_loop_main`.
  - `diag.py` — `diag_scan_main`, `diag_wide_main`, `diag_dump_main`.
  - `harvest_agent.py` is now a ~150-line thin entrypoint: nav_target.json dispatch
    (`main()`) + the faulthandler/crash.log wrapper. The deployer/host contract
    (nav_target/nav_status/STOP files, task name) is unchanged.
  - Every module uses the dual import pattern (`import offsets` in-guest, fall back to
    `guest_agent.offsets` repo-side), matching offsets.py from Session A.
- **harvest/__main__.py deploy_agent** — pushes the six new modules to C:\ib\agent\
  alongside harvest_agent.py (a deploy without them = ImportError in the guest).
- **guest_agent/ib_agent.py (P2.3)** — new `_tail_loop` daemon thread (started in
  `main()`): tails the freshest eq2log_*.txt IN-GUEST (open+seek incremental reads,
  0.25s cadence, re-picks the freshest log every 5s for character switches, reopens on
  truncation) and atomically mirrors the last 250 raw lines to C:\ib\combat_tail.txt.
  Never raises; idles if the log dir is missing.
- **agent/host_agent.py `_combat_log_loop` (P2.3)** — the per-second PS poll now reads
  the tiny combat_tail.txt when it's fresh (<5s old) and only falls back to
  `Get-Content -Tail 250` on the full log when the mirror is stale/absent (agent down).
  `_scan_combat_lines` unchanged — mirror lines keep the raw "(epoch)[date]" format, so
  recency/name/prepull parsing is identical.
- **shared/login.py (P2.5)** — new `_settle_until(cond, timeout)` helper; the blind
  Python-side settles now end early on their real condition with the OLD sleep as the
  timeout ceiling (worst case identical, best case seconds faster):
  post-LaunchPad 8s → PLAY-or-LOGIN-form visible; post-LP-credentials 6s → PLAY ready;
  post-LaunchPad-kill 2s → process actually gone (ceiling 5s).

## Decisions made

- Split boundaries chosen for zero import cycles: win_input ← eq2mem ← nav ←
  harvest_loops; diag ← eq2mem+agentio; agentio standalone. `_read_cstr` lives in
  eq2mem (diag imports it).
- All moved code is byte-for-byte VERBATIM (comments included) — no live testing today,
  so no behavior changes rode along with the split. Verified: AST cross-check that every
  cross-module import resolves, and that every top-level name of the old monolith exists
  somewhere in the split (NONE missing). pyflakes: only pre-existing unused locals.
- P2.3 consumer is the EXISTING host poll (reads the mirror file), not a new push
  channel — combat state keeps flowing to the brain via host_agent STATE_EVENTs, and
  the fallback keeps working with the agent down. A push path (guest → brain HTTP)
  would touch decision wiring; not without live testing.
- The AHK `Sleep` lines in login.py's snippets are NOT converted — they're the
  owner-validated Event-mode typing timing (Shift-race fix); only host-side Python
  settles changed. The 10s post-explorer "shell settle" stays blind: there's no cheap
  in-guest signal for "shell ready for AHK".

## State now

- Suite green (74 passed), compileall clean, pyflakes clean (2 pre-existing unused
  locals kept for verbatim fidelity). NOT live-validated.
- In flight: Session E (web_common shared static, UI cleanup, docs, hygiene) follows.

## Owner input needed (appended)

- First harvest "deploy agent" after this pulls the split modules into the guest —
  if a gather then fails to start, check C:\ib\crash.log / gdbg.log for ImportError
  (would mean a module didn't push).
- ib_agent needs its normal redeploy for the combat-tail mirror to exist; until then
  host_agent just keeps using the fallback tail poll (no regression).

## Commit(s)

- (this commit) — REFACTOR Session D: split harvest_agent into six modules + in-guest combat-log tail
