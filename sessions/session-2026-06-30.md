# Session Log — 2026-06-30 (catch-up synopsis)

> CATCH-UP LOG. Session files existed only through `session-2026-06-24.md`, but a large body of
> work landed 2026-06-25 → 2026-06-29 (harvest node-vs-mob RE, forge OCR/search hardening, batch
> combines, timing tuning, owner-standard shutdown) plus the 2026-06-23 **crafter** changes that
> the 06-23 log never captured (that log was harvest-only). This reconstructs all of it from the
> commit history + current live state so a future session can resume without re-deriving anything.

---

## A. Crafter station + counter work (2026-06-23, this conversation — was never logged)

### Changed
- `forge/recipes.py`
  - `_PRISTINE_ITEMS = ["backpack", "sack", "quiver"]` — sacks & quivers hit the same
    Pristine-recipe-vs-bare-objective mismatch as backpacks; without the prefix the OCR row-match
    fails and they never queue/craft. (commit `c7f8913`)
  - `recipe_station()` made **case-insensitive** (`st.setdefault(rn.lower(), …)` / `.get(name.lower())`)
    — bag/container recipes are stored lowercase in the scrape (`pristine rawhide leather backpack`)
    while most rows are Title-Case; exact-match was missing them.
- `forge/web/static/recipes.js` — the tray selection (`toggleSel`/`addRow`) and the outgoing
  payload (`selRows`) were dropping `station`, so **recipe-page lists arrived with no "table".**
  Now `station` rides along into the queue and saved lists.
- `forge/controller.py` — `set_queue()` belt-and-suspenders: `station = it.get("station") or
  recipes.recipe_station(name)`, so even older saved lists (no station) get a table on load.
- `forge/web/static/recipes.html` — cache-bust `recipes.js?v=20260623a` so browsers reload the JS.
- `config/forge/craft.yaml` — `reaction.counter_presses: 6` (doubled from default 3): counters
  weren't being mashed enough times to register. (commit `855cae8`)

### Owner's later tuning on top of the above (2026-06-26, already live — see § B)
- `press_interval: 0.12 → 0.3` (120 ms over-spammed the counter), `art_interval → 1.16`,
  `max_craft_time: 90 → 300`. **counter_presses stayed 6.**

---

## B. Forge / crafter hardening (2026-06-26 → 2026-06-29, no prior session file)

Theme: make OCR'd writ → search → recipe-pick → batch-count robust across classes, and tune the
craft loop timing. All commits are forge unless noted.

### Search & recipe-name resolution
- `97ea292` sage/scribe search types the **base name only**; the roman tier is matched by the
  recipe-row picker, not typed.
- `8c7c124` **GLOBAL search rules**: never type `(` `)` or `/`; suppress the quality tier in the
  typed query (the picker matches it). One rule for all classes.
- `5f2e044` / `69380fc` / `73eb44e` strip provisioner **serving/category words** from OCR'd writ
  names: `plate of` / `serving of`, then expanded to `cup/shot/stein/flask/glass/bowl/pot of`, and
  trailing category word (`Mountain Man drink` → `Mountain Man`).
- `9e6b580` / `4d78145` add `bottle of` to serving words + tolerate OCR `l→i/1` misread
  (`bott[il1]e`). (those two were staged-not-deployed at commit time — verify live.)
- `ec88a1e` tolerate OCR `glass→giass` (`g[li1|]ass`) in serving words.

### Pristine fallback (writ recipe not in the scraped DB)
- `e1003da` when a writ recipe isn't in the DB, **insert "pristine"** as a fallback so the search
  still targets the real recipe name.
- `5b90fb5` that fallback must **re-add the article** the live OCR drops:
  `Large Burlap Rug` → `a pristine large burlap rug`.

### Roman-tier picker (the recurring OCR headache)
- `0347d1e` recipe-row picker **respects the roman tier** (`Solar Flare IV` vs the no-tier base).
- `bc508c2` regression fix: the row blob is **space-stripped**, so a `\b` word-boundary reject
  broke it — picker matched wrong rows.
- `fb259ff` (`recipes.py` `_decompose`) **rescue an OCR-garbled trailing roman tier**
  (`lV`/`1V`/`iv`/`|V` → `IV`) so it isn't stripped down to the base recipe and mis-matched.

### Batch / combine counting (writ asks for N items, not N combines)
- `8eeb0ca` provisioner **food** batch yield = 2 → a "make 6" writ = **3 combines**, not 6.
- `30e3b00` woodworker **ammo** batch-craft 100/combine (per-recipe, not per-class).
- `3dee246` collapse batch recipes to **COMBINES at OCR-read time** so the dashboard shows the real
  combine count.

### Reflex / craft loop
- `0b19f8d` (`guest_agent/craft_reflex.py`) fix **greyed-template capture** (the once/writ
  pre-spam) + **auto twin-icon tiebreak** when two counter icons look alike.
- `e390955` / `7979130` / `aed147e` timing tune (see craft.yaml values below).

### Shutdown standard (applies to ALL web tools)
- `e3bbf03` **owner-standard Shutdown = `/camp desktop` → wait 25 s → power off VM.** Wired into
  forge (`controller._shutdown`/`_camp_desktop`, `/api/bot/<id>/shutdown`, `/api/shutdownall`) and
  the other web tools. `/camp desktop` cleanly exits the client to the OS before the VM powers off
  (no link-dead character left in-world). **This is the path used to shut the VMs down today.**

---

## C. Harvest node-vs-mob — SOLVED + targeting/nav (2026-06-25 → 2026-06-26, no prior session file)

The big RE win: reliably tell a **harvest node** from a **mob** render-object in client memory.

- `24a3359` add `dump` diag — full object dump w/ dereferenced strings to hunt the discriminator field.
- `4a9d623` auto-collect ground-truth-labelled object snapshots during gather → `study.jsonl`.
- `acead75` (disproven) drop node-vtable objects whose `+0x0C` flag is set — turned out wrong.
- `9b5a3ff` label the RE corpus off the **/consider verdict** (not harvest success); drop the
  disproven `+0x0C` filter.
- `901ec13` **SOLVE node-vs-mob**: discriminate on the **most-derived class vtable at `+0x140`**.
  This replaced the earlier blunt vtable-range + actor-proximity heuristics. *(Update the
  `harvest-memory-re` memory with this — it's the cracked discriminator.)*
- `00c17a0` self-correcting **mob blacklist** via /consider verdict.
- `5830d02` cap detour to detected candidates (`MAX_DETOUR=11m`) — stop beelining to far mobs …
- `ceaf9d7` … then **remove `MAX_DETOUR`** once mob-chasing was solved at the source — go to the
  nearest *real* node.
- `51328b1` **Tab-past-mob targeting** + stop suppressing real nodes that are merely blocked by mobs.
- Also in this window: `guest_agent/hud_overlay.py` (on-screen status HUD), `harvest/ogre_import.py`
  (import OgreNav graph data), `harvest/__main__.py` dashboard recorder growth, keymap web page.

---

## State now

- **Working / deployed live on 10.0.0.16 (`~/ib-app`, IB_FORGE_DIR=`~/ib-data/forge`):**
  - ib-forge restarted today and **active**, running all of § A + § B.
  - Live `craft.yaml reaction`: `press_interval: 0.15` (0.3 was still too slow for the counter slam),
    `counter_presses: 6`, `green_delta/red_delta: 16`.
  - END-OF-SESSION (06-30) additions on top of the above, all deployed live: woodworker ammo batch
    (`30e3b00`) was found NEVER-deployed and would've run a 300-arrow writ as 300 combines (~100x
    materials) — caught + deployed; `fb259ff` garbled-roman rescue; `bc508c2` picker regression fix.
    Both crafter VMs (iksar_buddy2/3) confirmed SHUT OFF after the push.
  - Live `craft.yaml timings`: `art_interval: 1.16`, `max_craft_time: 300`, `mana_wait: 25`.
  - Recipe-page lists now carry the crafting station ("table"); sacks/quivers craft like backpacks.
  - Harvest node-vs-mob discrimination solved (vtable `+0x140` most-derived class).
- **VMs:** both crafter doms (`iksar_buddy2`, `iksar_buddy3`) **cleanly camped + powered OFF today**
  via `/api/shutdownall`. `iksar_buddy` (base) and `satisfactory-server` were already off; the
  4070/homelab stack untouched.
- **Broken / watch:**
  - `9e6b580` / `4d78145` (`bottle of` serving word + OCR tolerance) were committed as
    *staged-not-deployed* — confirm they're actually in `~/ib-app` before relying on them.
  - Roman-tier OCR remains the fragile spot; several fixes layered (`0347d1e`→`fb259ff`) but
    2-line-wrapped imbued names still OCR poorly (pre-existing, low priority).
- **In flight:** none half-applied as of the restart; working tree is clean, all committed.

## Next
1. Owner end-to-end validation of a real provisioner/woodworker/tailor writ list now that station,
   pristine fallback, batch-combine, and serving-word strips are all live together.
2. Verify the two staged-not-deployed serving-word commits made it to `~/ib-app`.
3. Fold the harvest `+0x140` discriminator into the `harvest-memory-re` memory (mark node-vs-mob SOLVED).

## Owner input needed
- Confirm the crafter timing feel (`press_interval 0.3` / `art_interval 1.16`) on a live run.
- Any recipes still landing with no table after this restart → grab the OCR'd name so we can see if
  it's a DB-naming miss vs a payload bug.

## Commit(s)
- This conversation: `c7f8913` (sack+quiver pristine), `855cae8` (station→recipe-page lists +
  counter_presses 6). Earlier uncovered range catalogued above: `00c17a0`…`fb259ff`
  (2026-06-25 → 2026-06-29).
- No new code commit this session beyond the synopsis file; live restart + VM shutdown performed.
