# Session Log — 2026-06-23

## Scope this session
Make harvest navigation not run into walls (the "drunk animal" + zone-wall problem). Researched
how OgreBot/ISXEQ2 actually navigate, then built the OgreNav-style dense waypoint graph + A*.
Also: switched harvest char to Trailmix/robskin2004, which surfaced a stack of breakages
(login typing, position offset shifted, agent task disabled) — all run down and fixed.

## Changed
- `harvest/nav_graph.py` (NEW) — dense waypoint Graph + A* (pure stdlib; build host-side, path
  in-guest). `add_point(step=3, link=6)` meshes crossing passes; `route()` / `nearest()` / `astar()`.
- `harvest/record_graph.py` (NEW) — standalone host recorder (incremental save, SIGTERM). NOTE:
  superseded by the dashboard recorder below (standalone procs get reaped on the host when the
  launching SSH session ends; the dashboard persists).
- `harvest/__main__.py` — **dashboard-baked dense graph recorder** (the real recorder now):
  feeds every sensor push into a NavGraph while active; `graph_start/stop/_graph_feed/_graph_save`,
  `POST /api/graph {action:start|stop|status}`; checkpoints every 5 pts; APPENDS across sessions
  to `~/ib-data/harvest/graph_<zone>.json`. Passive accumulation — owner just harvests.
- `guest_agent/harvest_agent.py` — (1) harvest reworked to acquire-then-deplete on the HELD target
  (Ctrl+0 = target_nearest_npc+/consider, Tab-ring probe, Ctrl+9 = harvest current) so we never
  re-target off a node; (2) movement controller rewrite — turn key + body-frame strafe/forward
  decomposition (cos/sin of heading error) so she translates toward the target instead of
  turn-then-go ("drunk animal" fix); (3) graph nav: `goto()`/`reachable()`/`_nav_unstuck()`/
  `_jump()`/`load_graph()`, `gather_loop_main` rewritten to nearest-node-first + graph travel +
  ROAM off-path hop to nodes; (4) **`chat` mode** + `_eq2_window_any()`/`_eq2_pids()`/`type_chat()`
  to type slash commands (e.g. /loc) via keybd_event — self-serve, no flaky AHK; (5) POS/HDG
  offsets recalibrated.
- `harvest/sense_push.py`, `harvest/memory_read.py` — POS_OFF `0x1822b68→0x1822b78`,
  HDG_OFF `0x1822b74→0x1822b84` (client-update +0x10 shift; zone ptr unchanged). sense_push got
  Toolhelp `_eq2_pids()` pid-disambiguation (skip zombie procs).
- `forge/login.py` — login form: activate by `ahk_exe EverQuest2.exe` (not title) + robust field
  clear (End + BackSpace, not Ctrl+A). PARTIAL — see Broken.
- `~/ib-data/harvest/characters.yaml` (host, gitignored) — added Trailmix→robskin2004.

## Decisions made
- Heading memory-WRITE is DEAD (display copies, stomped <0.6s) → key-turn controller only.
- Nav = OgreNav-style recorded graph (owner-approved), NOT a navmesh. Owner's "cover the area,
  don't walk a thin loop" insight adopted → short off-path hops, walls mostly stop mattering.
- Recorder moved INTO the dashboard: standalone SSH-launched procs get reaped; dashboard persists
  and already has the push stream. Also realizes the "passive accumulation" approach.
- Login fix deferred to offline: it's a 1024x768-vs-1920x1080 OCR/gating mismatch (works on the
  crafter VMs). Owner logs in manually for now.

## State now
- Working: position/heading recalibrated + live (sensor pushing, 8 nodes). Self-serve /loc via
  agent chat mode. Dashboard dense-graph recorder LIVE and accumulating (passive). Harvest
  acquire-then-deplete logic. nav_graph A* (unit-tested).
- Broken / known issues: login automation on the 1920x1080 harvest VM (OCR gating mis-calibrated
  — [[login-resolution-mismatch]]). Deploys via ibhwbp_push sometimes silently don't take
  (re-verify after pushing). VM C:\ib may revert on reboot (re-deploy agent/sensor each boot).
  ibharv scheduled task can come up Disabled.
- In flight: graph nav (goto/gather_loop with ROAM) and the new movement controller are BUILT but
  UNTESTED live — waiting on the recorded graph. Owner is harvesting now to accumulate the graph.

## Next
- When owner says "done": `POST /api/graph stop` (saves), deploy `graph_<zone>.json` →
  `C:\ib\graph.json`, run a 1-lap gather test, watch movement + node grabs, tune ROAM/link/unstuck.
- Offline: fix login (set VM to 1024x768 OR scale login.py OCR/click coords).
- Update sensor LOG path to the active character for harvest detection (hardcoded Furyflatulence).

## Owner input needed
- The coverage harvest session (in progress) to build the graph. "done" when wrapped.

## Commit(s)
- `<hash>` — nav: OgreNav-style graph + dashboard recorder; recalibrate offsets (+0x10);
  self-serve /loc chat mode; movement turn+strafe controller; login ahk_exe/clear (partial)
