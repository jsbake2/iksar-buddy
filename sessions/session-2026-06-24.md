# Session Log — 2026-06-24

## Scope this session
Make the harvest bot actually work hands-off: detect ALL node types (not just ore/stone), stop
walking to monsters, move smoothly, harvest fully, run unattended for ~25 min with an on-screen
status HUD.

## Changed
- `guest_agent/harvest_agent.py`:
  - **NODE DETECTION REWRITE (the big one).** Was: vtable RANGE `0x1490000..0x14f0000`, pos `+0x1a0`,
    plus an actor-proximity filter. That range swept up MOB render-objects (walked to monsters) and
    `+0x1a0` HID bush/weed nodes (read a wrong/garbage pos), and the actor filter DELETED real nodes
    sitting near mobs. Now: EXACT vtable match `NODE_VTS = {0x14eb830, 0x14eba10}`, pos at `+0x60`.
    Confirmed by standing on each type: wood (felled high plains arbor), ore (wind swept stones),
    bush (high plains shrubbery) ALL share `0x14eb830`. Mobs are different vtables → no monster
    filter needed at all.
  - `_status()` helper: atomic + **non-fatal** write (write tmp, `os.replace`, swallow errors).
    Root-caused a hard crash: a long-running reader holds `nav_status.json` open → `PermissionError`
    on every write → the whole gather died (exit 1, no traceback). Status is dashboard-only; a locked
    status file must never kill the bot. Also mirrors to `C:\ib\hud.json` (clean, uncontended) for
    the overlay. All `Path(STATUS).write_text(...)` calls routed through `_status(...)`.
  - `faulthandler` + top-level crash log to `C:\ib\crash.log` (how I caught the PermissionError).
  - `done` set → **time-based dict** (`DONE_TTL=240s`) so respawned nodes get re-harvested on a long
    run (was: visited-once-forever, so it went idle after one lap).
  - **Camp-bail**: count consecutive `mob_blocked`; after 3, return so the tour walks out of a
    skeleton camp. Skeletons squat on real nodes and are NOT reliably in the actor list, so the
    actor-proximity "prefer clear nodes" filter misses them — the result-based counter is the robust
    fix. (Actor read kept too: `read_actors()` via the same heap scan, helps where mobs ARE visible.)
  - Scan cache widened (10m/4s → 30m/6s) to stop pause-y re-scans between near hops.
  - Straight-line node approach (`_nav_unstuck`) instead of graph-routing (the "jerks around between
    nodes" complaint — graph routing zigzagged through dense 3m mesh points).
  - `diag` mode = dump what production `read_node_array` sees; `diag2` = wide vtable scan to capture
    a missed node type's vtable (how bush was found at `+0x60`).
- `guest_agent/hud_overlay.py` (NEW): tkinter always-on-top borderless overlay, reads `hud.json`,
  shows state / target / harvest count, color-coded, draggable, Esc/right-click to close. Runs in
  session 1 via the new **`ibhud`** scheduled task (mirrors `ibharv`: user `iksar`, Interactive,
  Highest). Confirmed rendering top-right over the game via virsh screenshot.
- Host throwaway tooling in `/tmp` on workstation + `new-server`: `ib_push_agent.py` (push agent into
  VM, no relaunch), `ib_fire.py` (fire/stop gather), `ib_run.py` (fire arbitrary nav_target + read
  log), `ib_log.py` (read gdbg+status), `ib_diag.py`, `ib_guesttest.py`, `ib_hud_deploy.py`,
  `ib_monitor.py` (25-min watchdog: polls gdbg, stops on stall/crash/timeout, writes
  `/tmp/ib_monitor_result.txt`).

## Decisions made
- Exact-vtable detection over range+filter → eliminates BOTH "walks to mobs" and "skips bush/weed"
  with one change; the mob filter was the thing deleting real nodes.
- Status writes must be non-fatal (engineering) rather than chasing the file-lock holder — the bot's
  job is to gather, not to keep a UI file fresh.
- Camp-escape by counting harvest RESULTS, not by reading mobs from memory — the actor list does not
  reliably contain skeletons at a matching position (same gap that defeated the old filter).
- HUD overlay in tkinter (guest python 3.12.8 has tkinter); a separate `ibhud` task because
  guest-exec is session 0 and can't draw on the session-1 desktop.

## State now
- Working hands-off: detects wood/ore/bush nodes, smooth straight-line approach (dist_left ~0.2–0.9m),
  settle, deplete n=3, skip mob camps after 3 tries, respawn-aware, live on-screen HUD.
- Broken / known issues:
  - Wood arbors sometimes `done=gone n=0` even though consider says node — likely already-depleted
    husks within the respawn window OR the arbor `+0x60` origin is offset from the real interact
    point (trees are tall). Bush/ore deplete reliably; arbors are hit-or-miss. NEEDS a look.
  - `nav_status.json` is held open by some long-running guest process (sharing violation) so the
    HOST DASHBOARD's status is stale; the in-VM HUD reads `hud.json` and is fine. Find/!fix the
    lock holder (candidates: `C:\ib\paths.py` ×2, `sense_push.py`).
  - Camp-bail still does ~3 wasted `mob_blocked` per camp anchor; fine, not a trap anymore.
- In flight: a 25-min unattended gather + watchdog is RUNNING as of ~19:54. Watchdog auto-stops it
  and writes `/tmp/ib_monitor_result.txt` (reason, pulls, node types, last log).

## Next
- Read `/tmp/ib_monitor_result.txt` after the run; report yield + node-type breakdown.
- Fix arbor `gone n=0` (compare arbor `+0x60` to the consider/interact point; maybe a per-type pos
  offset, or detect "already depleted" and skip).
- Find the `nav_status.json` lock holder so the host dashboard shows live status again.
- Persist `ibhud` + HUD across VM reboot (add `hud_overlay.py` to dashboard `deploy_agent`; re-create
  task on launch). Same for re-enabling `ibharv` (comes up Disabled after reboot).
- Roadmap: player-proximity yield (stay ~10m from other players) — reuses `read_actors()`.

## Owner input needed
- He takes the VM for crafting after this run; STOP `ibharv` AND `ibhud` before handing back.
- SME: is "felled high plains arbor" a slow-respawn node, or should every targeted arbor harvest?

## Commit(s)
- (this commit) — node-detection rewrite (exact vtable @ +0x60), non-fatal status, HUD overlay,
  respawn TTL, camp-bail, 25-min watchdog tooling.
