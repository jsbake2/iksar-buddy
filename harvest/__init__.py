"""iksar_buddy harvest tool (ibh) — client-memory + navigation harvesting bot.

100% client-side: reads the EQ2 client process memory (pymem, in-guest) + the EQ2 log,
acts via keypresses. See HARVEST.md. Opsec: process title 'ibh', no eq2/bot visible names.
"""
