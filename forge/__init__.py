"""Forge — ib crafting automation (FORGE.md). Controls up to two craft guests
from one host-side process + web dashboard. This package is the dashboard +
telemetry + (later) the per-guest craft workers; for now the backend is mocked
(forge/sim.py) so the frontend is fully live and iterable."""
